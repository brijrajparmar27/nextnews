import styles from "../styles/Header.module.css"

const Header = ()=>{
    return <div className={styles.header}>
        <h2 className={styles.headerTitle}>React News</h2>
    </div>
}

export default Header;